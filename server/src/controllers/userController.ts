import { type NextFunction, type Request, type Response } from "express";

// /users/:id
// GET
// Find user by Id
export const findUserById = (
  req: Request,
  res: Response,
  next: NextFunction,
) => {};
